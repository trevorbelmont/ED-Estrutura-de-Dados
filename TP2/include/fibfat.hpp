#ifndef FIBFAT
#define FIBFAT

//Usando usigned long long as funções fatoriais, na arquitetura do meu chrome Os, vão até fat(65)

unsigned long long rFat(int n);

unsigned long long iFat (int n);

unsigned long long rFib(int n);

unsigned long long iFib(int n);

#endif